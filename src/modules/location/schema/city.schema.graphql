type City {
  id: ID!
  name: String!
}

type Query {
	cities: [City!]!
	city(id: ID!): City!
}

input CreateInputCity {
	name: String!
}

type Mutation {
	createCity(input: CreateInputCity!): City!
}